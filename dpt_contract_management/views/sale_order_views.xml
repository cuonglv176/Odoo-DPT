<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_sale_order_inherit_form" model="ir.ui.view">
        <field name="name">sale.order.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <header position="inside">
                <button name="create_new_contract" string="Create Contract"
                        type="object" class="oe_highlight"/>
                <button string="Cancel" type="action" name="805"
                        attrs="{'invisible': ['|', '|', '|', '|', ('id', '=', False), ('is_locked', '=', False), ('state', 'in', ('done','cancel')), ('confirm_cancel', '=', True), ('workorder_ids', '=', [])]}"/>
<!--                 attrs="{'invisible': ['|', '|', '|', ('id', '=', False), ('is_locked', '=', False), ('state', 'in', ('done','cancel')), ('confirm_cancel', '=', True)]}"/>-->

<!--                    remove workorder_count in buttot bellow-->
                <button string="Cancel" type="action"
                        name="805"
                        attrs="{'invisible': ['|', '|', '|', ('id', '=', False), ('is_locked', '=', False), ('state', 'in', ('done','cancel')), ('confirm_cancel', '=', False)]}"
                        confirm="Some product moves have already been confirmed, this manufacturing order can't be completely cancelled. Are you still sure you want to process ?"/>
            </header>
        </field>
    </record>
</odoo>